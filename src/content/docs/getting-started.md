# Getting Started

## Prerequisites

- A running instance of the backend service
- Access credentials provided by the admin team
- Python 3.11 or higher installed locally

## First Steps

1. Clone the repository
2. Copy `.env.example` to `.env` and fill in your credentials
3. Run `pip install -r requirements.txt`
4. Start the service with `python main.py`
